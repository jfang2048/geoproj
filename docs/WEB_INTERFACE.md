# Web interface

The current web interface is the React frontend in `frontend/`.

Start it with:

```bash
cd frontend
npm install
npm run dev
```

Use the browser workflow documented in `docs/USER_MANUAL.md`.
